mixin logo($img, $url)
    .logo&attributes(attributes)
        a.logo__link.is-transition(href= $url)
            img.logo__img(src=`./images/logo/${$img}` alt="ZmCrm")
            span.logo__text ZmCrm
